@using System.Collections.ObjectModel
@{
    Layout = "_Layout";
    if (!ViewData.ContainsKey("Sections"))
    {  
        ViewData["Sections"] = new Collection<KeyValuePair<string, string>>()
        {
            KeyValuePair.Create("/", "Home"),
            KeyValuePair.Create("/ingredients-selection", "Kill your leftovers 🔪"),
            KeyValuePair.Create("/back-office", "Back office 💀")
        };
    }
}
