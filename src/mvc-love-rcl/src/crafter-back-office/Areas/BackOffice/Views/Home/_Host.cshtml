@using System.Collections.ObjectModel

@{
    Layout = "_Layout";

    ViewData["Sections"] = new Collection<KeyValuePair<string, string>>()
    {
        KeyValuePair.Create("/", "Home"),
        KeyValuePair.Create("/back-office/ingredients-selection", "Ingredients Selection"),
        KeyValuePair.Create("/back-office/recipe-composition", "Recipe Composition")
    };
}

<component type="typeof(App)" render-mode="ServerPrerendered" />
